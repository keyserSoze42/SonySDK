apply plugin: 'java'
apply plugin: 'maven'

configurations {
    provided
    compile.extendsFrom provided

}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}
dependencies {
    provided files(  System.getenv("ANDROID_HOME") + '/platforms/android-23/android.jar')
}

